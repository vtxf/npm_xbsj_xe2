/**
 * 获取一堆位置数组的最小最大位置点
 * @param positions
 * @returns
 */
export declare function getMinMaxCoordinates(positions: [number, number][]): {
    minPos: [number, number];
    maxPos: [number, number];
};
